apiVersion: v1
kind: Service
metadata:
  name: ip-app-gateway-web-svc
spec:
  type: ClusterIP
  ports:
  - port: 13080
    targetPort: 8080
  selector:
    app: ip-app-gateway-web
  # type: NodePort
  # ports:
  #   - port: 80
  #     targetPort: 8080
  #     nodePort: 30180
  #     protocol: TCP
